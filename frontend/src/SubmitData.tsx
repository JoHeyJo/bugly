import React from "react";
import {postInfo} from './api'

type SubmitDataProp = {
  data: any;
}

/** Handles data submission for application's various components
 * [infoForm] => SubmitData
 */
function SubmitData({data}: SubmitDataProp){
  
}